# Project visuals

Drop real screenshots here and set the matching `image` path in `config/site.ts`.

Suggested names:

- `fire-nepal.jpg`
- `uvely-glow.jpg`
- `nepdealz.jpg`

Until a file is added, the site uses a stylized product frame instead of a fabricated screenshot.
